services:
  - type: web
    name: linkdropletbot
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: BOT_TOKEN
        value: your_actual_bot_token_here
      - key: PORT
        value: 8080
